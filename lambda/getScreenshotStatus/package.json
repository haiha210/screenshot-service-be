{
  "name": "get-screenshot-status-lambda",
  "version": "1.0.0",
  "description": "Lambda function to get screenshot status",
  "main": "app.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
