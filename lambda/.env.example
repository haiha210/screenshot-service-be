# Lambda Local Testing Environment Variables
# This file loads configuration from parent .env file

# AWS Configuration (for LocalStack)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_ENDPOINT=http://localhost:4566

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=screenshot-results

# SQS Configuration
SQS_QUEUE_URL=http://localhost:4566/000000000000/screenshot-queue

# Application Configuration
USE_LOCALSTACK=true
NODE_ENV=development

# Note: For actual values, the test script loads from ../.env file
